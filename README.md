# IntegrateGlobalsTask
Small backend service that ingests transaction data from a CSV file, stores hot data in Redis and historical data in MongoDB, and exposes a single HTTP API to query aggregated statistics over a date range
